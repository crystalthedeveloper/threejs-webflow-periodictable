// vite.config.js
import { defineConfig } from 'vite';

export default defineConfig({
  base: '/threejs-webflow-periodictable/', // Replace with your repository name
  build: {
    outDir: 'dist', // Output directory
  }
});
